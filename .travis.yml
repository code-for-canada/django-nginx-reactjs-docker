#Travis Configs File

services:
  - docker

matrix:
  include:
    # - language: python
    #   python: 3.6
    #   script:
    #     - docker-compose run --rm backend ./manage.py test backend/tests/

    - language: node_js
      node_js: "10.15"
      script:
        - docker-compose run --rm frontend yarn test -- --watch
