/*
The DOM window object provides access to the browser's session history 
through the history object. It exposes useful methods and properties that 
let you move back and forth through the user's history.

ref: https://developer.mozilla.org/en-US/docs/Web/API/History_API
*/
import { createBrowserHistory } from "history";

export default createBrowserHistory();
